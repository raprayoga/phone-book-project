import FormEdit from "@/components/modules/FormEdit"
import styled from "@emotion/styled"

const StyledFormEdit = styled(FormEdit)`
  max-width: 720px;
  margin: 30px auto;
`

export { StyledFormEdit }
